# گزارش مراحل ۱ تا ۴ — شار گرمایی Stage 2

تاریخ: ۲۰۲۶-۰۸-۱۸

## ۱) بازیابی سورس و تنظیمات

- اجرای اصلی: job array شمارهٔ `62716918`
- case شار گرما: task شمارهٔ `2`
- تعداد epoch واقعی: `30000`
- تعداد ذرات مرجع: `500000`
- تعداد نمونهٔ ارزیابی: `65536`
- مسیر سورس: `FP_PINN/pinn/cubic_homogeneous_3d/`
- commit بازسازی‌شده: `418c82a26fa6d9644938a784500cb156f455476b`
- خطای تاریخی ۳۰هزار epoch نسبت به ذرات: `12.2109776%`

اجرای ۵۰۰ epoch فقط pilot بوده و با نتیجهٔ ۳۰هزار epoch مخلوط نشده است.

## ۲) مرجع تحلیلی و ارزیابی

مرجع اصلی شار گرما اکنون جواب دقیق زیر است:

```text
Qx(t) = 0.25 exp(-4 nu t / 3)
```

در خروجی علاوه بر خطای L2 تحلیلی، نرخ واپاشی، `Pr_eff`، تاریخچهٔ ذره‌ای،
و نمودار سه‌تایی PINN/analytic/particle ثبت می‌شود. ذرات فقط cross-check هستند.

## ۳) loss و نمونه‌برداری

- weak loss حاصل از projection معادلهٔ FP روی ممان سوم فعال است.
- residual تحلیلی `dQx/dt + (4/3)nu Qx` بدون دادهٔ ذره‌ای اضافه شده است.
- نمونه‌برداری broad-tail با reweighting دقیق فعال است.
- تقارن محوری، antithetic sampling و چهار پنل چرخشی برای production فعال‌اند.

## ۴) گیت‌ها و اجرای production

- continuation: خطای تحلیلی حداکثر `5%`
- primary: حداکثر `2%`
- publication: حداکثر `1%`

گیت ۵٪ در submitter به‌صورت strict اعمال می‌شود؛ حتی در حالت NO-GO، بستهٔ خروجی
ساخته می‌شود. گیت‌های ۲٪ و ۱٪ جداگانه در `metrics.json` گزارش می‌شوند.

تست محلی: ۲۳ تست، صفر failure، ۷ تست TensorFlow که فقط روی محیط GPU یونیتی
اجرا می‌شوند. cross-check مستقل ۵۰۰هزارذره‌ای invariants را تا دقت ماشین حفظ
کرد و اختلاف ذره–تحلیل برای ممان سوم `2.591%` بود.

اجرای واقعی ۳۰هزار epoch باید روی GPU یونیتی انجام شود:

```bash
bash FP_PINN/pinn/cubic_homogeneous_3d/RUN_HEAT_FLUX_G0_UNITY.sh
```

این دستور فقط task شار گرما را submit می‌کند. شاخهٔ `main` تغییر داده نشده است.
